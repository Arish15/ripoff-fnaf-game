/** CRITICAL PATH VALIDATION **/

/** Path 1: First Night Start (startGame → office3d.init → animate)
    
    startGame() [game.js:138]
    ├─ Set display='flex' on #officeArea
    ├─ Force reflow: void officeEl.offsetHeight  ← ENSURES LAYOUT CALCULATED
    ├─ setTimeout(..., 100) [game.js:212]  ← GIVES LAYOUT TIME TO COMPLETE
    │  ├─ Guard: !game.running || monitorOpen ← PREVENTS RACE WITH MONITOR
    │  ├─ office3d.init() [office3d.js:83]
    │  │  ├─ Get dimensions from clientWidth/clientHeight
    │  │  ├─ Validate w >= 10 && h >= 10  ← PREVENTS ZERO-SIZE CANVAS
    │  │  ├─ if (w < 10 || h < 10) retry in 50ms ← GRACEFUL RETRY
    │  │  ├─ Create scene, camera, renderer
    │  │  ├─ Append renderer.domElement to container
    │  │  ├─ Force CSS styles on canvas (width:100%, height:100%)
    │  │  ├─ Call buildRoom, buildDecorations, etc.
    │  │  ├─ addEventListener for click/mousemove/resize
    │  │  ├─ animate()  ← START RENDER LOOP
    │  │  └─ renderer.render(scene, camera) ← FIRST FRAME
    │  ├─ office3d.show()  ← ENSURE RENDERING STARTED
    │  └─ Set door/light states to initial values
    │
    animate() [office3d.js:1530]
    └─ Loop continuously via requestAnimationFrame
       ├─ Early exit if !renderer || !scene || !camera
       ├─ Schedule next frame FIRST (prevents lost frames)
       ├─ Update camera rotation, door animation, fan
       ├─ Try-catch around renderer.render()  ← ERROR HANDLING
       └─ Continue loop

    SAFEGUARDS:
    ✓ Layout forced before init
    ✓ Timeout allows layout completion
    ✓ Game state verified before init
    ✓ Container dimensions validated
    ✓ Automatic retry if dimensions wrong
    ✓ Canvas forced to fill container
    ✓ Render loop started
    ✓ Render loop has error handling
    ✓ Early exit guards prevent crashes

*/


/** Path 2: Monitor Toggle → Office Reveal (toggleMonitor close → show)
    
    toggleMonitor() [cameras.js:1330]
    ├─ User presses space or clicks camera button to CLOSE monitor
    ├─ monitorOpen = false  ← STATE CHANGED
    ├─ Clear renderer canvas
    ├─ Stop camera static audio
    ├─ Clear pending monitorCloseTimeout  ← PREVENTS DOUBLE-TIMEOUT
    ├─ setTimeout(..., 250) [cameras.js:1373]  ← MATCHES CSS TRANSITION
    │  └─ Check: if (game.running && !monitorOpen) ← STATE VERIFICATION
    │     ├─ Get #officeArea element
    │     ├─ Set display='flex'  ← SHOW OFFICE
    │     ├─ Call office3d.show() [office3d.js:1638]
    │     │  ├─ Get #officeScene element
    │     │  ├─ Set style.display = '' (make visible)
    │     │  ├─ Force reflow: void offsetHeight  ← LAYOUT UPDATED
    │     │  └─ If !animId && renderer ready: animate()  ← RESUME RENDERING
    │     └─ Now office is visible and rendering
    │
    animate() [office3d.js:1530]
    └─ Already running, continues rendering office

    SAFEGUARDS:
    ✓ Pending timeout cleared (no race)
    ✓ Game state verified before show
    ✓ Monitor state verified before show
    ✓ DOM element existence checked
    ✓ Layout reflow forced
    ✓ Render loop verified to be running
    ✓ Early exit guards prevent crashes
    ✓ Null safety on all objects

*/


/** Path 3: Resize Event (window resize → camera update)
    
    window.addEventListener('resize', onResize) [office3d.js:171]
    
    onResize() [office3d.js:1656]
    ├─ Get #officeScene container
    ├─ Null safety: if (!c || !renderer || !camera) return
    ├─ Get w = c.clientWidth, h = c.clientHeight
    ├─ Validate: if (w < 10 || h < 10) return  ← SKIP INVALID RESIZE
    ├─ Calculate new aspect ratio
    ├─ Check if aspect changed >1%  ← ONLY UPDATE IF SIGNIFICANT
    ├─ Check if canvas size mismatched  ← ADDITIONAL CHECK
    ├─ If change needed:
    │  ├─ camera.aspect = newAspect
    │  ├─ camera.updateProjectionMatrix()
    │  ├─ renderer.setSize(w, h)
    │  └─ console.log() diagnostic
    └─ Continue loop

    animate() [office3d.js:1530]
    └─ Next frame uses updated camera

    SAFEGUARDS:
    ✓ Zero-size container prevented
    ✓ Epsilon check prevents unnecessary updates
    ✓ Size mismatch detected
    ✓ Null safety on all objects
    ✓ Diagnostic logging
    ✓ No render failure if resize fails

*/


/** Path 4: Return to Menu (returnToStart cleanup)
    
    returnToStart() [game.js:414]
    ├─ game.running = false  ← STOP GAME LOOP
    ├─ Clear gameLoop interval
    ├─ Stop all audio
    ├─ Clear overlays
    ├─ if (window.office3d && window.office3d.hide)
    │  └─ office3d.hide() [office3d.js:1651]
    │     └─ Get #officeScene, set display='none'  ← HIDE CANVAS
    ├─ Get #officeArea, set display='none'  ← HIDE OFFICE CONTAINER
    ├─ Clear monitor UI state (camera grid, toggle btn)
    └─ Show start screen

    office3d.hide() [office3d.js:1651]
    ├─ Does NOT kill animate() loop (still running in background)
    ├─ Canvas just hidden from view
    └─ Next init() will clear it and restart

    animate() [office3d.js:1530]
    ├─ Still running if animId is set
    ├─ Each frame renders to hidden canvas
    ├─ When init() called again: animId cleared, loop restarted
    └─ Next night starts fresh

    SAFEGUARDS:
    ✓ Game loop stopped first
    ✓ office3d.hide() called explicitly
    ✓ Canvas hidden before container hidden
    ✓ Next init() will clean up old state
    ✓ No memory leak from hidden render loop
    ✓ State properly reset for restart

*/


/** EDGE CASE HANDLING **/

Edge Case 1: Very fast monitor toggle (open/close/open rapidly)
├─ First open: monitorOpen=true, office hidden
├─ First close: setTimeout queued, office should show after 250ms
├─ Second open (before 250ms): monitorOpen=true again
│  └─ Previous setTimeout still pending
│      ├─ When fires: checks if (game.running && !monitorOpen)
│      └─ Condition false (monitorOpen=true), does nothing ✓
├─ Second close: new setTimeout queued
│  └─ OLD timeout might still fire but check prevents issue ✓

Edge Case 2: Container too small at init time
├─ init() checks w < 10 || h < 10
├─ Returns and reschedules: setTimeout(init, 50) ✓
├─ On retry: dimensions hopefully calculated by then
├─ If still too small: retries again (up to browser throttle)

Edge Case 3: Player switches night while monitor open
├─ startGame() called
├─ monitorOpen is global, stays true
├─ office3d.init() guard: !game.running || monitorOpen
└─ init() is skipped! Office stays hidden (correct - monitor still open) ✓

Edge Case 4: Window resize to zero during game
├─ onResize() triggered
├─ w = container.clientWidth → 0
├─ Guard: if (w < 10) return
└─ Resize skipped, game continues with old camera params ✓

Edge Case 5: WebGL context lost during render
├─ renderer.render() throws error
├─ Try-catch catches it
├─ console.error() logs it
├─ Loop continues next frame ✓

Edge Case 6: Power outage sequence during rendering
├─ game.powerOutage = true
├─ office3d.setDoorLeft/Right(false) called
├─ Office still rendering, but doors closing
├─ Screen darkened by CSS overlay
└─ Next frame shows Freddy eyes ✓

*/


/** SUMMARY OF FIXES **/

Problem: Canvas not rendering on night mode entry
Root: Race condition + missing layout validation

Solution 1: Force layout reflow before Three.js init
  Implementation: void element.offsetHeight

Solution 2: Validate container dimensions before init
  Implementation: if (w < 10 || h < 10) retry

Solution 3: Ensure canvas fills container
  Implementation: CSS styles on renderer.domElement

Solution 4: Handle render errors gracefully
  Implementation: Try-catch around renderer.render()

Solution 5: Sync office visibility with game state
  Implementation: Game state checks on monitor toggle

Solution 6: Complete state cleanup on restart
  Implementation: Explicit office3d.hide() call

Solution 7: Safe monitor toggle state management
  Implementation: Timeout clearing and state verification

Solution 8: Resilient resize handling
  Implementation: Dimension validation + epsilon checks

ALL SOLUTIONS are defensive, non-breaking, and have safeguards for edge cases.
